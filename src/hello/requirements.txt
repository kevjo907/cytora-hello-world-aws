# Runtime dependencies for the Lambda package.
#
# Deliberately empty: the handler uses only the Python standard library.
# Nothing to install means a tiny artifact, fast cold starts, and no
# third-party CVEs riding along into production.
